<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.3.3.RELEASE</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>site.yangpan</groupId>
    <artifactId>yangpan-dubbo</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>yangpan-dubbo</name>
    <description>Demo project for Spring Boot</description>
    <packaging>pom</packaging>

    <properties>
        <java.version>1.8</java.version>
    </properties>

    <dependencyManagement></dependencyManagement>
    <modules>
        <module>yangpan-dubbo-api</module>
        <module>yangpan-dubbo-provider</module>
        <module>yangpan-dubbo-consumer</module>
        <module>yangpan-dubbo-registry-zookeeper-provider</module>
        <module>yangpan-dubbo-registry-zookeeper-consumer</module>
        <module>yangpan-dubbo-registry-nacos-provider</module>
        <module>yangpan-dubbo-registry-nacos-consumer</module>
    </modules>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
